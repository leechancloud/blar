import secrets
import string

characters = string.ascii_letters + string.digits + "!@#$%^&*"
length = int(input("비밀번호 길이를 입력하세요: "))

if length < 1:
    print("길이는 1 이상이어야 합니다.")
else:
    password = "".join(secrets.choice(characters) for _ in range(length))
    print("생성된 비밀번호:", password)
